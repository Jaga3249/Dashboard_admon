import { Typography } from "antd";
import React from "react";

const Customers = () => {
  return (
    <div>
      <Typography.Title level={4}>custumers</Typography.Title>
    </div>
  );
};

export default Customers;
